let input: unknown = "Hello, TypeScript!";

if (typeof input === "string") {
  console.log(input.toUpperCase()); // Output: HELLO, TYPESCRIPT!
}

// Unknown.ts
// The 'unknown' type in TypeScript is similar to 'any', but it requires type checking before performing operations on the variable.
