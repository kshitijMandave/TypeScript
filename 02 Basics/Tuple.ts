// Tuple.ts
// Tuples in TypeScript allow you to define an array with fixed types for each element.
// This is useful when you want to enforce a specific structure for an array.
let userInfo: [string, number] = ["Alice", 30];

console.log(userInfo); // Output: ["Alice", 30]
