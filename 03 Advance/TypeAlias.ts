// Type Alias Means : we can create our custom type.

type value = string | number | null;

let a: value;

// With Function Examples
type arg = string | null;

function abcd(obj: arg) {}

abcd("Kshitij");
