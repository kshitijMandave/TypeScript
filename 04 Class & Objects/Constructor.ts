class BottleMaker {
  constructor(public name: String, public price: number) {} //3. the data will come here. when we call the class constructor will execute first.
} //1. created class

let b1 = new BottleMaker("Milton", 1200); //2. when we call this will go to the constructor
let b2 = new BottleMaker("Cello", 3200); //2. when we call this will go to the constructor
