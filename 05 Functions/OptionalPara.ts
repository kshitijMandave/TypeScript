// Optional Parameter

function abcd(name: string, age: number, gender?: string) {}

abcd("Kshitij", 24, "Male");

abcd("Pratik", 24); // if we add ? (question mark before semicolan then it will be optional, Do ya Na Do)
